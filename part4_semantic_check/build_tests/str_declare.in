
x[8] static int;
int f(int e, int d) {
    char h <= 'h';

    {
        d = x[0];
        h = 'c';
        x[0] = 10;
        f(d, x[0]);

        char r;

        input x[0];
        //input 0;
        //input (123 + x[0]);
        //input c;

        //output 0,0,0;
        //output r;
        //output 'c';

        for (int i, x[0] = 0, d << 2: 1 < 0: i = i + 1) {
            int k;
            int p;

            {
                int g;
            output x[0];
            };
        };
    };

    return 5;
}