
x[8] static int;
int f(int e, int d) {
    char h <= 'h';

    {
        d = x[0];
        h = 'c';
        x[0] = 10;
        f(d, x[0]);

        char r;

        input x[0];
        //input 0;
        //input (123 + x[0]);
        //input c;

        output x[0];
        output 0,0,0;
        //output r;
        //output 'c';
    };
}